import React, {FC ,memo} from 'react';

const Header:FC = memo(()=> {

  return (
    <header>
      <span>Insurance Portal</span>
      <span>Hi, Agent</span>
    </header>
  )
})

export default Header;
